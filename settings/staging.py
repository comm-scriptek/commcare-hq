"""This module sets staging specific settings. It will need to define
the staging specific database and different email targets etc. but
should be very similar to production."""
from production import *
